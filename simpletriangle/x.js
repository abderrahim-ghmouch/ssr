function drawx(num) {
    let stars=""

    for (let i = 0; i <= num; i++)
    {
        for (let j = 0; j <= num; j++)
        {
            while (let k= 0 && k !== i) {
                stars +=""  
            }
        }
        
    }
}